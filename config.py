# 브랜드 컬러 정의
class BRAND_COLORS:
    # 1. 아이덴티티 컬러 (메인/서브)
    MAIN_MINT = "#13d6a2"      # 사용자 선택 (최고점, 활성화 버튼)
    SUB_MINT = "#0fbc8b"       # 조금 더 깊은 민트 (호버, 중간 강조)
    SUB2_MINT = "#B5F8EF"
    POINT_CORAL = "#ff7e67"    # 보색 포인트 (주의, 삭제, 혹은 강조 대비용)

    # 2. 텍스트 컬러
    SUB_TEXT= "#7C8085"
    SUB2_TEXT = "#94A3B8" # 더 보조적인 설명용

    # 3. 다크모드 대응 배경/텍스트
    DARK_BG = "#0f172a"        # 사이드바/다크 배경
    DARK_CARD = "#1e293b"      # 카드/박스 배경
    DARK_TEXT = "#f8fafc"      # 주 텍스트
    DARK_SUBTEXT = "#94a3b8"   # 보조 텍스트 (설명문)

    # 4. 라이트모드 대응 배경/텍스트
    LIGHT_BG = "#ffffff"
    LIGHT_CARD = "#f1f5f9"
    LIGHT_TEXT = "#1e293b"
    LIGHT_SUBTEXT = "#64748b"

    # 5. 상태 알림 (Success/Error 등)
    # Streamlit 기본 컬러보다 우리 브랜드와 더 잘 어울리는 톤으로 지정
    STATE_SUCCESS = "#10b981"  # 성공 (안정적인 초록)
    STATE_ERROR = "#ef4444"    # 에러 (강렬한 레드)
    STATE_WARNING = "#f59e0b"  # 주의 (차분한 주황)

    # 6. 무채색 보조 (데이터가 없거나 배경이 필요할 때)
    # rgba(0,0,0,0) 대신 변수명을 써서 가독성을 높입니다.
    EMPTY_GREY = "#e2e8f0"     # 데이터 없음/비활성화 상태
    TRANSPARENT = "rgba(0,0,0,0)" 

    # [차트용]
    # 1. 단일 강조색 (막대 그래프 등)
    CHART_MAIN = ["#13d6a2"] 
    
    # 2. 순차적 그라데이션 (연령대, 구독 수 등 위계가 있는 데이터)
    # 진한 민트에서 연한 민트로 이어지는 구성
    CHART_SEQUENTIAL = [
        "#13d6a2", # 메인 (가장 높은 값)
        "#0fbc8b", 
        "#34d399", 
        "#6ee7b7", 
        "#a7f3d0", 
        "#ecfdf5"  # 가장 연한 색
    ]
    
    # 3. 범주형 팔레트 (성별, 직업 등 독립된 항목 비교)
    # 메인 민트를 중심으로, 핑크/오렌지 톤을 차분하게 섞은 조합
    CHART_CATEGORICAL = [
        "#13d6a2", # 1. 메인 민트
        "#ffb3a7", # 2. 차분한 코랄 핑크
        "#ffd8b1", # 3. 부드러운 오렌지
        "#94e2d5", # 4. 연한 청민트
        "#7a82b4", # 5. 물빠진 네이비
        "#94b8e2"  # 6. 스카이 블루
    ]

    # 4. 대비감 팔레트 (선버스트)
    # 중간값이 제일 안보이게
    SUNBURST_SCALE = [
            [0.0, "#FFDCDC"],  # 최저점 (연핑크)
            [0.25, "#FFEEE7"], 
            [0.5, "#EAFFFD"], 
            [0.75, "#B5F8EF"], 
            [1.0, "#25F1BE"]   # 최고점 (민트)
    ]

    # 5. 이진 분류 (예/아니오, 만족/불만족 등)
    # 범주형 팔레트의 1번(민트)과 2번(코랄핑크)을 활용해 명확히 대비
    CHART_BINARY = ["#13d6a2", "#ffb3a7"]